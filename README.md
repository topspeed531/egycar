# EgyCar 🇪🇬

Arabic RTL car marketplace starter built with Next.js 14 + TypeScript + Tailwind.

## Run
npm install
npm run dev

Then open http://localhost:3000

This version includes a professional homepage, filters, car cards, responsive design, and car detail pages. The demo inventory uses sample image URLs/data and is not connected to a real database yet.
